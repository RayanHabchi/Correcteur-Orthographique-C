#ifndef LEVENSHTEIN_H
#define LEVENSHTEIN_H

int L/*evenshtein*/(char * un, char * deux);
/**
 * BUT: fonction qui va determiner la distance minimale entre deux chaines de caractere
 * en utilisant un algorithme iteratif utilisant des tableaux bi-dimensionel évitant de faire 
 * plusieurs fois les mêmes calculs
 * PARAMETRES: les deux chaines de caractères à comparer
 */
int minimum(int a, int b, int c);
/**
 * BUT: renvoie le minimum entre 3 entiers
 * 
 * PARAMETRES: les entiers a,b,c à comparer
 */

#endif